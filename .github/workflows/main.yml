# This is a basic workflow to help you get started with Actions

name: CI

on:
  schedule:
  - cron:  '*/15 * * * *'

  workflow_dispatch:

jobs:
  check:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Run a one-line script
        run: git diff --stat 'qa@{now}..main@{1 day ago}' 2>/dev/null | wc -l
